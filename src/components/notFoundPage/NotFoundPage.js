const NotFoundPage = () => (
    <div className="flex justify-center items-center h-screen w-screen">
        <p className="uppercase font-bold text-6xl text-red-700">404 page not found</p>
    </div>
);

export default NotFoundPage;
